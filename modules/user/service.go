package user

// Service contains all biz logic functioneleties to reduce coupling from http layer
type Service struct {
	//DB *redis.Conn `inject:""`
}

/* func (r *Service) FindByTimeStamp(since int64) (*helpers.Model, error) {
	r.DB.Do()
} */
